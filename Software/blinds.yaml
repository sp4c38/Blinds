substitutions:
  dev: "Leon Blinds"
  PIN_RELAY_UP:    "4"    # GPIO4
  PIN_RELAY_DOWN:  "0"    # GPIO0 (boot strap pin)
  PIN_RELAY_POWER: "5"    # GPIO5
  PIN_IR:          "13"
  PIN_IR_PWR:      "14"

  dead_time_ms: "300"
  extra_close_ms: "4500" # later set to 56000ms
  extra_open_ms:  "300"
  max_run_ms: "60000"

  ir_warmup_ms:   "120"     # time to let IR settle before we rely on it
  ir_failsafe_ms: "180000"  # turn IR off after 3 min max, just in case

esphome:
  name: blinds-leon

  on_boot:
    # make sure the IR LED is OFF after boot
    priority: -100
    then:
      - switch.turn_off: ir_led
      - binary_sensor.template.publish:
          id: ir_present
          state: OFF

esp8266:
  board: esp12e

# Enable logging
logger:

# Enable Home Assistant API
api:
  # Encrypt all the traffic between the ESP and Home Assistant. You need to set the same key in Home Assistant during setup.
  encryption:
    key: !secret api_encryption_key

ota:
  platform: esphome
  password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Blinds Leon Fallback Hotspot"
    password: !secret wifi_ap_password

web_server: # Enable the optional web server
  port: 80    
  version: 3
  local: true
  auth:
    username: !secret web_user
    password: !secret web_pass

switch:
  - platform: gpio
    id: relay_power
    name: "${dev} Motor Power"
    pin: ${PIN_RELAY_POWER}
    restore_mode: ALWAYS_OFF

  - platform: gpio
    id: relay_up
    name: "${dev} Dir UP"
    pin: ${PIN_RELAY_UP}
    restore_mode: ALWAYS_OFF
    interlock: [relay_down]
    interlock_wait_time: ${dead_time_ms}ms

  - platform: gpio
    id: relay_down
    name: "${dev} Dir DOWN"
    pin: ${PIN_RELAY_DOWN}
    restore_mode: ALWAYS_OFF
    interlock: [relay_up]
    interlock_wait_time: ${dead_time_ms}ms

  # IR LED power
  - platform: gpio
    id: ir_led
    name: "${dev} IR LED"
    pin: ${PIN_IR_PWR}
    restore_mode: ALWAYS_OFF

binary_sensor:
  - platform: gpio
    id: btn_wall
    name: "${dev} Wall Button"
    pin:
      number: 12
      mode: INPUT
    on_press:
      then:
        - if:
            condition:
              lambda: 'return !id(is_opening) && !id(is_closing);'
            then:
              - if:
                  condition:
                    lambda: 'return id(ir_present).state;'
                  then:
                    - script.execute: start_open
                  else:
                    - script.execute: start_close
            else:
              - script.execute: stop_motor
  - platform: gpio
    id: ir_raw
    internal: true
    pin:
      number: ${PIN_IR}
      mode: INPUT
      inverted: true
    filters:
      - delayed_on: 20ms
      - delayed_off: 20ms
    on_press:
      then:
        - lambda: |-
            if (id(ir_led).state) {
              id(ir_last_state) = true;
              id(ir_present).publish_state(true);
            }
        - if:
            condition:
              lambda: 'return id(is_closing);'
            then:
              - script.execute: after_detect_close
    on_release:
      then:
        - lambda: |-
            if (id(ir_led).state) {
              id(ir_last_state) = false;
              id(ir_present).publish_state(false);
            }
        - if:
            condition:
              lambda: 'return id(is_opening);'
            then:
              - script.execute: after_detect_open

  - platform: template
    id: ir_present
    name: "${dev} IR Present"
    lambda: |-
      return id(ir_last_state);

# ------------------ Flags + scripts ------------------
globals:
  - id: is_opening
    type: bool
    initial_value: "false"
  - id: is_closing
    type: bool
    initial_value: "false"
  - id: ir_last_state
    type: bool
    initial_value: "false"

script:
  # Power IR briefly before motion and arm a failsafe auto-off
  - id: ir_power_on
    mode: restart
    then:
      - switch.turn_on: ir_led
      - delay: ${ir_warmup_ms}ms
      - script.execute: ir_auto_off

  - id: ir_auto_off
    mode: restart
    then:
      - delay: ${ir_failsafe_ms}ms
      - if:
          condition:
            lambda: 'return !id(is_opening) && !id(is_closing);'
          then:
            - switch.turn_off: ir_led

  - id: watchdog
    mode: restart
    then:
      - delay: ${max_run_ms}ms
      - script.execute: stop_motor

  - id: stop_motor
    mode: restart
    then:
      - lambda: |-
          id(is_opening) = false;
          id(is_closing) = false;
      - script.stop: after_detect_close
      - script.stop: watchdog
      - switch.turn_off: relay_power
      - delay: ${dead_time_ms}ms
      - switch.turn_off: relay_up
      - switch.turn_off: relay_down
      - switch.turn_off: ir_led       # ensure IR is OFF when idle

  - id: start_open
    mode: restart
    then:
      - script.stop: after_detect_close
      - script.stop: after_detect_open
      - script.stop: watchdog
      - lambda: |-
          id(is_opening) = true;
          id(is_closing) = false;
      - script.execute: ir_power_on
      - switch.turn_off: relay_down
      - delay: ${dead_time_ms}ms
      - switch.turn_on: relay_up
      - switch.turn_on: relay_power
      - script.execute: watchdog

  - id: start_close
    mode: restart
    then:
      - script.stop: after_detect_close
      - script.stop: after_detect_open
      - script.stop: watchdog
      - lambda: |-
          id(is_opening) = false;
          id(is_closing) = true;
      - script.execute: ir_power_on
      - switch.turn_off: relay_up
      - delay: ${dead_time_ms}ms
      - switch.turn_on: relay_down
      - switch.turn_on: relay_power
      - script.execute: watchdog
      - delay: 30ms
      - lambda: |-
          if (id(ir_present).state || id(ir_raw).state) {
            id(after_detect_close).execute();
          }

  - id: after_detect_open
    mode: restart
    then:
      - delay: ${extra_open_ms}ms
      - if:
          condition:
            lambda: 'return id(is_opening);'
          then:
            - script.execute: stop_motor
            - cover.template.publish:
                id: blind
                state: OPEN

  - id: after_detect_close
    mode: restart
    then:
      - delay: ${extra_close_ms}ms
      - if:
          condition:
            lambda: 'return id(is_closing);'
          then:
            - lambda: |-
                id(ir_last_state) = true;
                id(ir_present).publish_state(true);
            - cover.template.publish:
                id: blind
                state: CLOSED
            - script.execute: stop_motor

# ------------------ HA cover ------------------
cover:
  - platform: template
    id: blind
    name: "${dev}"
    device_class: blind
    has_position: false
    lambda: |-
      // IR present => CLOSED, otherwise OPEN
      return id(ir_present).state ? COVER_CLOSED : COVER_OPEN;
    open_action:
      - script.execute: start_open
    close_action:
      - script.execute: start_close
    stop_action:
      - script.execute: stop_motor